function Nav() {
    return (
        <nav className="flex">
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Menu</a>
            <a href="#">Reservations</a>
            <a href="#">Order online</a>
            <a href="#">Login</a>
        </nav>
    );
}

export default Nav;